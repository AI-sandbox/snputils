# API Reference

The API reference is grouped by task so common objects and functions are not buried in a module tree. Module-level pages at the end list lower-level package entry points.

```{toctree}
:maxdepth: 2

snp
ancestry
phenotypes
ibd
processing
stats
tools
simulation
datasets
visualization
cli
modules
```

For the complete symbol index, see the {ref}`genindex` or the {doc}`modules` page.
